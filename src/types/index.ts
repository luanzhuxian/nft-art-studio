export type Nft = {
  tokenId: string;
  tokenUri: string;
  name: string;
  descriptipn: string;
  imageUri: string;
  uri: string; // artical uri
  type: string; // image, pdf, mp3...
};

export type NftMeta = {
  name: string;
  descriptipn: string;
  imageUri: string;
  uri: string; // artical uri
  type: string; // image, pdf, mp3...
};
